var myCarousel = new bootstrap.Carousel(
  document.getElementById("projectCarousel"),
  {
    interval: 5000, // Set the interval for automatic sliding (milliseconds), or remove this line for manual control
  }
);
